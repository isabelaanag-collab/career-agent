__pycache__/
*.pyc
.pytest_cache/

# credenciais — nunca commitar
google_credentials.json
.env
# qualquer JSON solto na raiz (ex.: chave de service account baixada do Google
# Cloud) — os JSONs legítimos do projeto ficam em data/, tests/fixtures/ etc.
/*.json
